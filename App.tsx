import { useEffect,useState } from "react";
import { Alert,StyleSheet,View } from "react-native";
import { StatusBar } from "expo-status-bar";
import { Schoolbell_400Regular,useFonts } from "@expo-google-fonts/schoolbell";
import type { Session } from "@supabase/supabase-js";
import { AuthScreen } from "./components/AuthScreen";import { DogOnboardingScreen } from "./components/DogOnboardingScreen";import { DogManagerModal } from "./components/DogManagerModal";import { HomeScreen } from "./components/HomeScreen";import { AppTab,HubScreen } from "./components/HubScreen";import { HistoryScreen } from "./components/HistoryScreen";import { MeScreen } from "./components/MeScreen";import { defaultWalkTitle,WalkCompleteScreen } from "./components/WalkCompleteScreen";import { WalkingScreen } from "./components/WalkingScreen";import { useWalkTracker } from "./hooks/useWalkTracker";import { supabase } from "./lib/supabase";import { fetchDogBadges,syncDogBadges } from "./services/badges";import { fetchDogsForUser } from "./services/dogs";import { createWalk,fetchWalks } from "./services/walks";import { DogBadge } from "./types/badge";import { Dog } from "./types/dog";import { Walk,WalkTag } from "./types/walk";
export default function App(){const[fontsLoaded]=useFonts({Schoolbell_400Regular});const[authReady,setAuthReady]=useState(false),[session,setSession]=useState<Session|null>(null);const[dogs,setDogs]=useState<Dog[]>([]),[dogsLoading,setDogsLoading]=useState(false),[activeDogId,setActiveDogId]=useState<string|null>(null),[dogManagerOpen,setDogManagerOpen]=useState(false),[allWalks,setAllWalks]=useState<Walk[]>([]),[badges,setBadges]=useState<DogBadge[]>([]);const[tab,setTab]=useState<AppTab>("home"),[shareRoute,setShareRoute]=useState(false),[walkTags,setWalkTags]=useState<WalkTag[]>([]),[walkTitle,setWalkTitle]=useState("");const{isWalking,walkFinished,seconds,distance,points,startWalk,stopWalk,resetWalk}=useWalkTracker();useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setAuthReady(true)});const{data:a}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);setAuthReady(true)});return()=>a.subscription.unsubscribe()},[]);useEffect(()=>{if(!session?.user.id){setDogs([]);setAllWalks([]);setBadges([]);setActiveDogId(null);setTab("home");return}loadDogs(session.user.id);loadWalks()},[session?.user.id]);const activeDog=dogs.find(d=>d.id===activeDogId)||dogs[0];const walks=activeDog?allWalks.filter(w=>w.dog_id===activeDog.id||(!w.dog_id&&w.dog_name===activeDog.name)):[];useEffect(()=>{if(activeDog?.id)loadBadges(activeDog.id)},[activeDog?.id]);async function loadDogs(userId:string){setDogsLoading(true);try{const next=await fetchDogsForUser(userId);setDogs(next);setActiveDogId(current=>current&&next.some(d=>d.id===current)?current:next[0]?.id??null)}catch(error){console.error("Load dogs error:",error)}finally{setDogsLoading(false)}}async function loadWalks(){try{const next=await fetchWalks();setAllWalks(next);return next}catch(error){console.error("Load walks error:",error);return []}}async function loadBadges(dogId:string){try{setBadges(await fetchDogBadges(dogId))}catch(error){console.error("Load badges error:",error)}}function beginWalk(shouldShare=false){setShareRoute(shouldShare);setWalkTags([]);setWalkTitle("");setTab("home");startWalk()}function discardWalk(){setWalkTags([]);setWalkTitle("");resetWalk()}async function saveWalk(){if(!activeDog){Alert.alert("No dog selected","Add a walking buddy first.");return}if(!session?.user.id){Alert.alert("Session expired","Please sign in again before saving this walk.");return}try{await createWalk({userId:session.user.id,dogId:activeDog.id,dogName:activeDog.name,title:walkTitle.trim()||defaultWalkTitle(new Date()),distanceKm:distance,durationSeconds:seconds,routePoints:points,shareRoute,tags:walkTags});const nextAll=await loadWalks();const dogWalks=nextAll.filter(w=>w.dog_id===activeDog.id);await syncDogBadges(activeDog.id,dogWalks);await loadBadges(activeDog.id);Alert.alert("Saved!",`${activeDog.name} walked ${distance.toFixed(2)} km 🐕`);setWalkTags([]);setWalkTitle("");resetWalk()}catch(error){const message=error instanceof Error?error.message:"Unknown error";console.error(error);Alert.alert("Save failed",message)}}async function signOut(){const{error}=await supabase.auth.signOut();if(error)Alert.alert("Sign out failed",error.message)}if(!fontsLoaded||!authReady)return <View style={styles.container}/>;if(!session)return <View style={styles.container}><AuthScreen/><StatusBar style="dark"/></View>;if(dogsLoading)return <View style={styles.container}/>;if(dogs.length===0)return <View style={styles.container}><DogOnboardingScreen userId={session.user.id} onCreated={()=>loadDogs(session.user.id)}/><StatusBar style="dark"/></View>;let content;if(walkFinished)content=<WalkCompleteScreen seconds={seconds} distance={distance} points={points} dogName={activeDog.name} title={walkTitle} shareRoute={shareRoute} tags={walkTags} onTitleChange={setWalkTitle} onTagsChange={setWalkTags} onShareRouteChange={setShareRoute} onSave={saveWalk} onDiscard={discardWalk}/>;else if(isWalking)content=<WalkingScreen seconds={seconds} distance={distance} points={points} dogName={activeDog.name} onStopWalk={stopWalk}/>;else if(tab==="me")content=<MeScreen dog={activeDog} walks={walks} badges={badges} onNavigate={setTab} onStartWalk={beginWalk} onSignOut={signOut}/>;else if(tab==="map")content=<HistoryScreen dog={activeDog} walks={walks} badges={badges} onNavigate={setTab} onStartWalk={beginWalk}/>;else if(tab!=="home")content=<HubScreen tab={tab} walks={walks} badges={badges} dog={activeDog} onNavigate={setTab} onStartWalk={beginWalk} onSignOut={signOut}/>;else content=<HomeScreen walks={walks} dog={activeDog} onStartWalk={beginWalk} onNavigate={setTab} onOpenDogs={()=>setDogManagerOpen(true)}/>;return <View style={styles.container}>{content}<DogManagerModal visible={dogManagerOpen} userId={session.user.id} dogs={dogs} activeDogId={activeDog.id} onClose={()=>setDogManagerOpen(false)} onChanged={()=>loadDogs(session.user.id)} onSelect={setActiveDogId}/><StatusBar style="dark"/></View>}
const styles=StyleSheet.create({container:{flex:1,backgroundColor:"#F8F3E9",paddingHorizontal:18,paddingTop:68,paddingBottom:8}});